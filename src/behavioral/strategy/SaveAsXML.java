package behavioral.strategy;

public class SaveAsXML implements ExportStrategy {
    @Override
    public boolean save(String data) {

        // do the operation of saving data as XML
        // return true if operation was successful.

        return false;
    }
}
