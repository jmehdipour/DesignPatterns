package behavioral.strategy;

public class SaveAsJSON implements ExportStrategy {
    @Override
    public boolean save(String data) {

        // do the operation of saving data as JSON
        // return true if operation was successful.

        return false;
    }
}
