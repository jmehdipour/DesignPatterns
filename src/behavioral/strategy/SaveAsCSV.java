package behavioral.strategy;

public class SaveAsCSV implements ExportStrategy {
    @Override
    public boolean save(String data) {

        // do the operation of saving data as CSV
        // return true if operation was successful.

        return false;
    }
}
